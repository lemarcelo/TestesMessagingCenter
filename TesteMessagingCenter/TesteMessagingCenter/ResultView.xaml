<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TesteMessagingCenter.ResultView">
    
    <StackLayout Spacing="15" HorizontalOptions="Center">
        <Button Text="Voltar" Clicked="Voltar" BackgroundColor="BlueViolet" />
        <Button Text="Somente Subscribe" Clicked="RegistrarMensagem" BackgroundColor="YellowGreen" />
        <Button Text="Somente Send" Clicked="EnviarMensagem" BackgroundColor="LightGreen" />
        <Button Text="Somente Unsubscribe" Clicked="ApagarRegistro" BackgroundColor="Red" />
        <BoxView BackgroundColor="Transparent" HeightRequest="20" />
        <Button Text="Voltar Com Subscribe, Send e Ubsubscribe" Clicked="GoPaginaMainComRegistro" BackgroundColor="Green"/>
        <BoxView BackgroundColor="Transparent" HeightRequest="20" />
        <Entry Text="{Binding Texto}" Placeholder="Texto da Mensagem" />
        <Button Text="Voltar com Texto" Clicked="VoltarTexto" BackgroundColor="Gray" />

        </StackLayout>
    </ContentPage>